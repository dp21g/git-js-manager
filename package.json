{
  "name": "git-squash-ui",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"node --watch server/index.mjs\" \"vite\"",
    "build": "vite build",
    "start": "node server/index.mjs",
    "setup": "npm install"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.1.0",
    "concurrently": "^8.2.0",
    "svelte": "^4.2.0",
    "vite": "^5.4.0"
  }
}
